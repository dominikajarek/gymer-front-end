import React from 'react';

export const AddNewSlot = (props) => {
    return (
        <div>
            <h1>partner id = {props.partnerId}</h1>
            <h2>feature still in progress</h2>
        </div>
    )
}